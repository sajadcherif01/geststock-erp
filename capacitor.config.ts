import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ma.geststock.erp',
  appName: 'GestStock ERP',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
